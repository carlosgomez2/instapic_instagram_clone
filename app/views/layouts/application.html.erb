<!DOCTYPE html>
<html>
<head>
  <title>Instapic</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <nav class="navBar">
      <nav class="wrapper">
        <div class="logo">
          <%= link_to root_path, class: "pure-menu-heading" do %>
            <%= image_tag('logo.svg', "data-svg-fallback" => image_path('logo_il.svg'),:style => "width: 100px;") %>
          <% end %>
        </div>
        <input type="checkbox" id="menu-toggle">
          <label for="menu-toggle" class="label-toggle"></label>
        </input>
        <div class="mid-links">
          <ul>
            <% if user_signed_in? %>
              <li><%= link_to "New Pic", new_pic_path, class: "menu-link" %></li>
              <li><%= link_to "Config", edit_user_registration_path, class: "menu-link" %></li>
              <li><%= link_to "Log out", destroy_user_session_path, :method => :delete, class: "menu-link" %></li>
            <% else %>
              <li><%= link_to "Log in", new_user_session_path, class: "menu-link" %></li>
              <li><%= link_to "Sing up", new_user_registration_path, class: "menu-link" %></li>
            <% end %>
          </ul>
        </div>
      </nav>
    </nav>


  </header>

  <div class="main-wrapper">
    <div class="content">

      <div id="alert-wrapper">
        <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, class: "pure-alert" %>
        <% end %>
      </div>

      <%= yield %>
    </div>
  </div>

</body>
</html>
